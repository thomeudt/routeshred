import React from 'react';
import { t } from '../i18n';

function RouteDetail() {
  return (
    <div>
      <h1>{t('route.details')}</h1>
      <p>{t('route.detailsComingSoon')}</p>
    </div>
  );
}

export default RouteDetail;
